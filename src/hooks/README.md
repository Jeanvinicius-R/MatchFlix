# hooks

Hooks React reutilizáveis entre features (ex.: `useDebouncedValue`, `useMediaQuery`). Ainda vazio — será populado quando surgir interatividade client-side compartilhada (busca, player, favoritos).
